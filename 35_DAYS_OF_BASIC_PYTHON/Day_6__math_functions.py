import math

pi = 3.14
x = 1
y = 2
z = 3

#print(round(pi)) # will round to the nearest number
#print(math.ceil(pi)) # will round a number UP to the nearest whole integer
#print(math.floor(pi)) # rounded down
#print(abs(pi)) # abs will give the absolute value of a number
#print(pow(pi,2)) # pow will raise a base number to a power
#print(math.sqrt(pi)) # will give the squareroot of the number
#print(max(x,y,z)) # will find the largest of the values
print(min(x,y,z)) # will fin the lowest pf the values