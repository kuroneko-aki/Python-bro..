# function = a block of code which is executed only whet it is called

def hello(first_name, last_name, age):
    print("hello "+first_name+" "+last_name)
    print("you are "+str(age)+" years old")
    print("you old as fvkc!")

hello("Aki", "Hayakawa", 21) # arguments = are the information u r sending to a function, but a matching number of parameters r needed